{% section 'breadcrumb' %}
<div class="ltn__login-area pb-65">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="section-title-area text-center">
          <h1 class="section-title">{{ 'customer.activate_account.title' | t }}</h1>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-lg-6 offset-lg-3">
        <div class="account-login-inner">
            {% form 'activate_customer_password',class : 'ltn__form-box contact-form-box' %}
              <input type="password" value="" name="customer[password]" id="CustomerPassword" placeholder="{{ 'customer.activate_account.password' | t }}">
          	  <input type="password" value="" name="customer[password_confirmation]" id="CustomerPasswordConfirmation" placeholder="{{ 'customer.activate_account.password_confirm' | t }}">
              <div class="btn-wrapper mt-0">
                <button class="theme-btn-1 btn btn-block" type="submit">{{ 'customer.activate_account.submit' | t }}</button>
              </div>
              <div class="go-to-btn mt-20">
                <a href="/account/login"><small>{{ 'customer.activate_account.cancel' | t }}</small></a>
              </div>
          {% endform %}    
        </div>
      </div>
    </div>
  </div>
</div>
