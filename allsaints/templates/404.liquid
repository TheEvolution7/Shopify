{% include 'breadcrumb' %}
<section class="pt-10 pb-10">
  <div class="container">
    <h2 class="fs-sm-40 mb-11 text-center">{{ 'general.404.title' | t }}</h2>
    <div class="row overflow-hidden">
      <div class="col-md-12">
        <p style="text-align:center;">{{ 'general.404.subtext' | t }}</p>
        <p style="text-align:center;">
          <a href="{{ routes.root_url }}" class="btn btn--has-icon-after">{{ 'general.404.link' | t }} {% include 'icon-arrow-right' %}</a>
        </p>
      </div>
    </div>
  </div>
</section>